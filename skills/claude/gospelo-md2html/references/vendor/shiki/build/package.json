{
  "name": "shiki-build",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild entry.mjs --bundle --minify --format=iife --outfile=shiki.min.js"
  },
  "dependencies": {
    "esbuild": "0.28.2",
    "shiki": "4.4.3"
  }
}
